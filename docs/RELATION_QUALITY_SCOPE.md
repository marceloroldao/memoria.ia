# Relation quality scope

This slice does not perform semantic fact merging. It only prevents context-dependent relation endpoints from entering durable relation candidates while preserving explicit compact facts.
