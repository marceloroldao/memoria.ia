name: patterns v1.06 regression

on:
  push:
    branches:
      - experiment/patterns-v106
  workflow_dispatch:

concurrency:
  group: patterns-v106-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  regression:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e '.[product,test]'
      - name: Full regression
        run: python -m pytest -q
      - name: v1.05 episodic baseline
        run: python -m pytest -q tests/test_episodes_v105.py
      - name: v1.06 recurring pattern gate
        run: python -m pytest -q tests/test_patterns_v106.py
