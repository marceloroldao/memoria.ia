name: provenance confidence v1.13 regression

on:
  push:
    branches:
      - experiment/provenance-confidence-v113
  workflow_dispatch:

concurrency:
  group: provenance-confidence-v113-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  regression:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e '.[product,test]'
      - name: Full regression
        run: python -m pytest -q
      - name: v1.12 temporal structural baseline
        run: python -m pytest -q tests/test_temporal_structural_inference_v112.py
      - name: v1.13 provenance confidence gate
        run: python -m pytest -q tests/test_provenance_confidence_v113.py
