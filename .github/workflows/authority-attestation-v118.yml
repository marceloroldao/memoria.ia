name: authority attestation v1.18 regression

on:
  push:
    branches:
      - experiment/authority-attestation-v118
  workflow_dispatch:

concurrency:
  group: authority-attestation-v118-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  regression:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e '.[product,test]'
      - name: Full regression
        run: python -m pytest -q
      - name: v1.17 authority independence baseline
        run: python -m pytest -q tests/test_authority_independence_v117.py tests/test_adjudication_cycle_guard_v116.py
      - name: v1.18 authority attestation gate
        run: python -m pytest -q tests/test_authority_attestation_v118.py
