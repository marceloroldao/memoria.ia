name: Role joint boundary length normalized

on:
  push:
    branches: [experiment/bdr-primary-linux]
    paths:
      - 'benchmarks/role_joint_boundary_length_normalized.py'
      - 'benchmarks/role_joint_boundary_robustness.py'
      - 'benchmarks/role_permutation_cost_matrix.py'
      - 'benchmarks/role_directional_relation_matrix.py'
      - '.github/workflows/role-joint-length-normalized.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: .:src
    steps:
      - uses: actions/checkout@v4
      - name: Install package
        run: python -m pip install -e '.[product]'
      - name: Compare total vs mean contradiction cost
        run: |
          mkdir -p benchmark-results
          python benchmarks/role_joint_boundary_length_normalized.py | tee benchmark-results/role-joint-length-normalized.json
      - uses: actions/upload-artifact@v4
        with:
          name: role-joint-length-normalized
          path: benchmark-results/role-joint-length-normalized.json
